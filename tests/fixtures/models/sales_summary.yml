description: 'Sales summary aggregated by product'
sql: |
  SELECT 
    sale_id as product_id,
    COUNT(*) as total_sales,
    COUNT(*) as total_quantity,
    SUM(amount) as total_revenue
  FROM sales_data 
  GROUP BY sale_id
  ORDER BY total_revenue DESC